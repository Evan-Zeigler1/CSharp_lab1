
namespace CSharp_lab1
{
    class Program
    {
        static void Main(string[] args)
        {
            /*C# is an elegant object-oriented language that enables developers to build a variety of secure and 
           robust applications that run on the .NET Framework.*/
           
             int num;
            num = 42;
            
            bool a = false;
            double b = 4.22;
            string c = "Hi";
            int d = 11; ;
            
             /*To create a C# program, you need to install an integrated development environment (IDE) with coding and 
            debugging tools.*/
            
             Console.WriteLine("Learning C#");
             
            string temp;
            temp = Console.ReadLine();

            /*
             Declaring an integer
              and printing it 
            */
            int x = 42;
            Console.WriteLine(x);

            n1 = true;
            Console.WriteLine(n1);
            
            const int num = 2;

            int x = 42;
            int y = 7;
            int z = y
            * y;
            Console.WriteLine;

            int x = 42;
            x / = 3;

             static void Main(string[] args)
            {
                string msg = "Hello";
                Console.WriteLine(msg);
            }

        }
    }
}
